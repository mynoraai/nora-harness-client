# Journey 17 - Settings Extensions ACP Runtime Management Removed

This is the text interaction description for the corresponding HTML wireframe, not a PM decision summary. It describes entry, visible states, completion, and recovery.

- Source HTML: [17-settings-extensions-acp-runtime-management-removed.wireframe.html](17-settings-extensions-acp-runtime-management-removed.wireframe.html)
- Status: Status: removed
- Verification note: Marked removed; do not present as current Settings surface.

## User Goal

The old Extensions/ACP runtime management journey is removed from the active journey set and kept as a historical marker.

## Entry And Preconditions

- Entry: Removed journey
- Initial user state: Extensions ACP runtime management is not an active current flow.
   - Do not add it to Settings as if it exists.
   - Keep replacement work in current provider/settings flows.

## Interaction Flow

1. Removed journey
   - User sees: Extensions ACP runtime management is not an active current flow.
   - Do not add it to Settings as if it exists.
   - Keep replacement work in current provider/settings flows.
2. Current replacement
   - User sees: Use Providers, Permissions, or Code runtime states for related work.
   - Historical note only.

## Branches And Recovery States



## End State

- Ends at: Current replacement
  - Use Providers, Permissions, or Code runtime states for related work.
   - Historical note only.

## Notes

- User outcome: Removed extension/runtime management does not pollute current settings wireframes.
- Constraints: No active ACP UI is invented.
- Evidence gaps: Need product decision if this surface returns.
