# Journey 11 - Cloud Deploy Removed

This is the text interaction description for the corresponding HTML wireframe, not a PM decision summary. It describes entry, visible states, completion, and recovery.

- Source HTML: [11-cloud-deploy-removed.wireframe.html](11-cloud-deploy-removed.wireframe.html)
- Status: Status: removed
- Verification note: Marked removed; do not draw active deployment UI without user confirmation.

## User Goal

Cloud deploy is kept only as a removed historical journey marker so it does not appear as an active current product flow.

## Entry And Preconditions

- Entry: Removed journey
- Initial user state: Cloud deploy is not part of the current journey set.
   - Do not include cloud deploy as active onboarding/code flow.
   - Point users to current local Code or publish flows.

## Interaction Flow

1. Removed journey
   - User sees: Cloud deploy is not part of the current journey set.
   - Do not include cloud deploy as active onboarding/code flow.
   - Point users to current local Code or publish flows.
2. Replacement path
   - User sees: Use local Code workspace and publish flows when a release artifact is needed.
   - Replacement is documented, not redesigned here.

## Branches And Recovery States



## End State

- Ends at: Replacement path
  - Use local Code workspace and publish flows when a release artifact is needed.
   - Replacement is documented, not redesigned here.

## Notes

- User outcome: Removed flows stay visible as historical markers only.
- Constraints: No active UI is invented.
- Evidence gaps: Need product decision if cloud deployment returns later.
